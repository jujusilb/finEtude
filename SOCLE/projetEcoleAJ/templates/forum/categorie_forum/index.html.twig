{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libellé</th>
                <th>Forum</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for categorieForum in categorieForums %}
                <tr>
                    <td>{{categorieForum.id}}</td>
                    
                    <td>
                        <a href="{{ path('categorieForum_affichage', {'id': categorieForum.id}) }}">
                            {{ categorieForum.libelle }}
                        </a>
                    </td>
                    <td>{{  categorieForum.forum.libelle }}</td>
                    <td>
                        <a href="{{ path('categorieForum_affichage', {'id': categorieForum.id}) }}" class="btn btn-secondary">lire</a>
                        <a href="{{ path('categorieForum_edition', {'id': categorieForum.id}) }}" class="btn btn-secondary">editer</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('categorieForum_nouveau') }}" class="btn" >Creer Nouvel admin</a>
{% endblock %}
