<form method="post" action="{{ path('categorieForum_suppression', {'id': categorieForum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorieForum.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
