<form method="post" action="{{ path('statutOuvrage_suppression', {'id': statutOuvrage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statutOuvrage.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
