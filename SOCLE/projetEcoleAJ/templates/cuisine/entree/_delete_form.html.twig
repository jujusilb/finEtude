<form method="post" action="{{ path('entree_suppression', {'id': entree.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entree.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
