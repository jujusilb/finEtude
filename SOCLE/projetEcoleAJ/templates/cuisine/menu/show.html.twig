{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Menu</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ menu.id }}</td>
				</tr>
				<tr>
					<th>Entrée</th>
					<td>{{ menu.entree.libelle }}</td>
				</tr>
				<tr>
					<th>Plat</th>
					<td>{{ menu.plat.viande.libelle }} - {{ menu.plat.legume.libelle }}</td>
				</tr>
				<tr>
					<th>Fromage</th>
					<td>{{ menu.fromage.libelle }}</td>
				</tr>
				<tr>
					<th>Dessert</th>
					<td>{{ menu.dessert.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('menu_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('menu_edition', {'id': menu.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('cuisine/menu/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
