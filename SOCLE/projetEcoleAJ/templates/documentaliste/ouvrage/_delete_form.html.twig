<form method="post" action="{{ path('ouvrage_suppression', {'id': ouvrage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ouvrage.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
