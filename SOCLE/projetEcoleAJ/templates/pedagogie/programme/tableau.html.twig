{% extends 'base.html.twig' %}

{% block title %}Hello programmeController!{% endblock %}

{% block body %}
[[print_r(programme)]]
 <a href="{{ path('programme_nouveau') }}" class="btn" >Creer Nouveau programme</a>
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Promotion</th>
                <th>Professeur</th>
                <th>Matiere</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
{% for programme in programmes %}
        <tr>
            <td>{{ programme.getPromotion().libelle }}</td>     {# Assuming 'nom' is a property of 'Professeur' #}
            <td>{{ programme.getProfesseur().prenom }} {{ programme.getProfesseur().nom }}</td>     {# Assuming 'nom' is a property of 'Professeur' #}
            <td>{{ programme.getMatiere().libelle }}</td>    {# Assuming 'libelle' is a property of 'Matiere' #}
            {#<td>
                <a href="{{ path('programme_affichage', {'id': programme.id}) }}" class="btn btn-secondary">lire</a>
                <a href="{{ path('programme_edition', {'id': programme.id}) }}" class="btn btn-secondary">editer</a>
            </td>#}
        </tr>
    {% endfor %}
    </tbody>
</table>
<a href="{{ path('programme_nouveau') }}" class="btn">Creer Nouveau programme</a>
        </tbody>
    </table>
   
{% endblock %}
