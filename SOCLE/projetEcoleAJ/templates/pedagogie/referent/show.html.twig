{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Referent</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ referent.id }}</td>
				</tr>
				<tr>
					<th>professeur</th>
                    <td>
                        <a href="{{ path('professeur_affichage', {'id': referent.professeur.id}) }}">
                            {{ referent.professeur.nom }} {{referent.professeur.prenom}}
                        </a>
                    </td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('referent_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('referent_edition', {'id': referent.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('pedagogie/referent/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
