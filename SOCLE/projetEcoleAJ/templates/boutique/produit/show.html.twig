{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Produit</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ produit.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ produit.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('produit_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('produit_edition', {'id': produit.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('cantine/produit/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
