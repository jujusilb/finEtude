{% extends 'base.html.twig' %}

{% block title %}Stage
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Entree</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ stage.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ stage.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('stage_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('stage_edition', {'id': stage.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('stage/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
