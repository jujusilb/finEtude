<form method="post" action="{{ path('secretariat_suppression', {'id': secretariat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ secretariat.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
