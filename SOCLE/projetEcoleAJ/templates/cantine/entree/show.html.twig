{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Entree</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ entree.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ entree.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('entree_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('entree_edition', {'id': entree.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('cantine/entree/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
