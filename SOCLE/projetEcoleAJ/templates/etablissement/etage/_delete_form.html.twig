<form method="post" action="{{ path('etage_suppression', {'id': etage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etage.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
