<form method="post" action="{{ path('pole_suppression', {'id': pole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pole.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
