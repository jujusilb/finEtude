{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Admin</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ admin.id }}</td>
				</tr>
				<tr>
					<th>photo</th>
					<td><img src="{{ vich_uploader_asset(admin, 'imageFile') }}" class="col-md-6 float-md-end mb-3 ms-md-3" style="width: 100px; height: 100px; object-fit: cover;" alt="..."></td>
				</tr>
								<tr>
					<th>Nom</th>
					<td>{{ admin.nom }}</td>
				</tr>
								<tr>
					<th>Prenom</th>
					<td>{{ admin.prenom }}</td>
				</tr>
								<tr>
					<th>email</th>
					<td>{{ admin.email }}</td>
				</tr>
								<tr>
					<th>Password</th>
					<td>{{ admin.password }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('admin_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('admin_edition', {'id': admin.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('admin/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
