{% extends 'base.html.twig' %}

{% block title %}Exercice
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Exercice</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ note.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ note.libelle }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('note_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('note_edition', {'id': note.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('pedagogie/note/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
