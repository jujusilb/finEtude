<form method="post" action="{{ path('professionnel_suppression', {'id': professionnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ professionnel.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
