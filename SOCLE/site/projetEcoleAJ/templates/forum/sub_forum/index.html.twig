{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libellé</th>
                <th>CategorieForum</th>
            </tr>
        </thead>
        <tbody>
            {% for subForum in subForums %}
                <tr>
                    <td>{{subForum.id}}</td>
                    <td>{{ subForum.libelle }}</td>
                    <td>{{ subForum.CategorieForum.libelle }}</td>
                    <td>
                        <a href="{{ path('subForum_affichage', {'id': subForum.id}) }}" class="btn btn-secondary">lire</a>
                        <a href="{{ path('subForum_edition', {'id': subForum.id}) }}" class="btn btn-secondary">editer</a>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('subForum_nouveau') }}" class="btn" >Creer Nouvel admin</a>
{% endblock %}
