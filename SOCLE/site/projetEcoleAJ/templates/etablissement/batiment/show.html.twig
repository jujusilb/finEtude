{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Batiment</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ batiment.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ batiment.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('batiment_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('batiment_edition', {'id': batiment.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('etablissement/batiment/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
