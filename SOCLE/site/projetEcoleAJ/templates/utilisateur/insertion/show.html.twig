{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Insertion</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ insertion.id }}</td>
				</tr>
				<tr>
					<th>photo</th>
					<td><img src="{{ vich_uploader_asset(insertion, 'imageFile') }}" class="col-md-6 float-md-end mb-3 ms-md-3" style="width: 100px; height: 100px; object-fit: cover;" alt="..."></td>
				</tr>
								<tr>
					<th>Nom</th>
					<td>{{ insertion.nom }}</td>
				</tr>
								<tr>
					<th>Prenom</th>
					<td>{{ insertion.prenom }}</td>
				</tr>
								<tr>
					<th>email</th>
					<td>{{ insertion.email }}</td>
				</tr>
								<tr>
					<th>Password</th>
					<td>{{ insertion.password }}</td>
				</tr>
								<tr>
					<th>date d'embauche</th>
					<td>{{ insertion.dateEmbauche |date('d/m/Y')}}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('insertion_index') }}" class="btn btn-secondary">liste Insertion</a>

		<a href="{{ path('insertion_edition', {'id': insertion.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('utilisateur/insertion/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
