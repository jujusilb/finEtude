<form method="post" action="{{ path('viande_suppression', {'id': viande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ viande.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
