{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Commande</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ commande.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ commande.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('commande_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('commande_edition', {'id': commande.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('cantine/commande/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
