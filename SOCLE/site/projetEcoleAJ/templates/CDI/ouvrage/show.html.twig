{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Message</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ ouvrage.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ ouvrage.titre }}</td>
				</tr>
				<tr>
					<th>Categorie</th>
					<td>
					{% for categorieOuvrage in ouvrage.categorieOuvrages %}
					{{ categorieOuvrage.libelle }}
					{%endfor%}
					</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('ouvrage_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('ouvrage_edition', {'id': ouvrage.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('CDI/ouvrage/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
