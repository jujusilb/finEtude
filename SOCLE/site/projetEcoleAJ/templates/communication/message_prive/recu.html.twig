<table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Expediateur</th>
                <th>Sujet</th>
                <th>Contenu</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for messagePriveRecu in messagePriveRecus %}
                <tr>
                    <td>{{messagePriveRecu.id}}</td>
                    <td>
                        <a href="{{ path('membre_affichage', {'id': messagePriveRecu.expediteur.id}) }}">
                            {{ messagePriveRecu.expediteur.prenom }} {{ messagePriveRecu.expediteur.nom }}
                        </a>
                    </td>
                    <td>{{ messagePriveRecu.sujet }}</td>
                    <td>{{ messagePriveRecu.contenu }}</td>
                    <td>
                        <a href="{{ path('messagePrive_affichage', {'id': messagePriveRecu.id}) }}" class="btn btn-secondary">lire</a>
                        <a href="{{ path('messagePrive_edition', {'id': messagePriveRecu.id}) }}" class="btn btn-secondary">editer</a>
                    </td>

                </tr>
            {% endfor %}
        </tbody>
    </table>