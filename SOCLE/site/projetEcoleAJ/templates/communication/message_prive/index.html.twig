{% extends 'base.html.twig' %}

{% block title %}Hello messagePriveController!{% endblock %}

{% block body %}
    <table class="table table-striped table-bordered table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Expediateur</th>
                <th>Sujet</th>
                <th>Contenu</th>
                <th>Destinataire</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for messagePrive in messagePrives %}
                <tr>
                    <td>{{messagePrive.id}}</td>
                    <td>
                            <a href="{{ path('membre_affichage', {'id': messagePrive.expediteur.id}) }}">
                                {{ messagePrive.expediteur.prenom }} {{ messagePrive.expediteur.nom }}
                            </a>
                    </td>
                    <td>{{ messagePrive.sujet }}</td>
                    <td>{{ messagePrive.contenu }}</td>
                    <td>
                        <a href="{{ path('membre_affichage', {'id': messagePrive.destinataire.id}) }}">
                            {{ messagePrive.destinataire.prenom }} {{ messagePrive.destinataire.nom }}</td>
                        </a>
                    </td>                    
                    <td>
                        <a href="{{ path('messagePrive_affichage', {'id': messagePrive.id}) }}" class="btn btn-secondary">lire</a>
                        <a href="{{ path('messagePrive_edition', {'id': messagePrive.id}) }}" class="btn btn-secondary">editer</a>
                    </td>

                </tr>
            {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('messagePrive_nouveau') }}" class="btn" >Creer Nouveau messagePrive</a>
{% endblock %}
