<form method="post" action="{{ path('messageGeneral_suppression', {'id': messageGeneral.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messageGeneral.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
