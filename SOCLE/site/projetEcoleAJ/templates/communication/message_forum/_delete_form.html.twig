<form method="post" action="{{ path('messageForum_suppression', {'id': messageForum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messageForum.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
