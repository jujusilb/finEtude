{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Plat</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ plat.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ plat.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('plat_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('plat_edition', {'id': plat.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('cuisine/plat/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
