{% extends 'base.html.twig' %}

{% block title %}Matiere
{% endblock %}

{% block body %}

	<div class="container">

		<h5>Legume</h5>
		<hr style="width:100%; height:5px; background-color:#1B4F72 ">

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ legume.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ legume.libelle }}</td>
				</tr>
			</tr>
			</tbody>
		</table>

		<a href="{{ path('legume_index') }}" class="btn btn-secondary">Liste de sieges</a>

		<a href="{{ path('legume_edition', {'id': legume.id}) }}" class="btn btn-secondary">Modifier</a>

		{{ include('cantine/legume/_delete_form.html.twig') }}
	</div>
	<br>
	<br>
	<br>
	<br>
	<br>
	<br>

{% endblock %}
